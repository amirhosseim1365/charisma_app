export const API_BASE_URL = 'https://amirmoharreri.cfd';

const config = {
  ENDPOINTS: {
    LOGIN: '/api/v1/auth/telegram-login',
    COMPLETE_PROFILE: '/auth/complete-profile',
    USER_PROFILE: '/auth/users/me',
  },
  // Add other configuration constants here
};

export default config; 